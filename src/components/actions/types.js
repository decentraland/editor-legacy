export const saveScene = {
  request: '[Request] Save scene to IPFS',
  success: '[Success] Save scene to IPFS',
  failed: '[Failed] Save scene to IPFS'
};

export const bindName = {
  request: '[Request] Bind the hash to IPNS',
  success: '[Success] Bind the hash to IPNS',
  failed: '[Failed] Bind the hash to IPNS'
};

export const saveMeta = {
  request: '[Request] SAVE metadata',
  success: '[Success] SAVE metadata',
  failed: '[Failed] SAVE metadata'
};

export const saveMetaManyParcels = {
  request: '[Request] SAVE metadata for multiple parcels',
  success: '[Success] SAVE metadata for multiple parcels',
  failed: '[Failed] SAVE metadata for multiple parcels'
};

export const loadMeta = {
  request: '[Request] Load metadata',
  success: '[Success] Load metadata',
  failed: '[Failed] Load metadata'
};

export const connectWeb3 = {
  request: '[Request] Connect to web3 provider',
  success: '[Success] Connect to web3 provider',
  failed: '[Failed] Connect to web3 provider'
};

export const fetchBalance = {
  request: '[Request] Fetch balance',
  loadedBalance: '[Request] Amount loaded',
  success: '[Success] Fetch balance',
  failed: '[Failed] Fetch balance'
};

export const loadParcel = {
  request: '[Request] Load parcel',
  requestMany: '[Multiple Request] Load parcels',
  many: '[Multiple Success] Load parcels',
  success: '[Success] Load parcel',
  failed: '[Failed] Load parcel'
};

export const balanceParcel = {
  success: '[Success] Balance parcel retrieved',
  failed: '[Failed] Balance parcel fetch fail'
};
